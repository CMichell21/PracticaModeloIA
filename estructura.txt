# Mostrar los textos de los casos F
columnas_mostrar = [
    "DESCRIPCION_INCOMMING",
    "DESCRIPCION_BANCO",
    "VALOR_ESPERADO"
]

df_f = df[columnas_mostrar].copy()

df_f["SIMILITUD_COSENO"] = similarities

df_f = df_f[
    df_f["VALOR_ESPERADO"]
    .astype(str)
    .str.strip()
    .str.upper() == "F"
]

print("\n========================================")
print("DETALLE DE LOS CASOS F")
print("========================================")

print(
    df_f.sort_values("SIMILITUD_COSENO").to_string(index=False)
)

print("========================================")
