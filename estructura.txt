df_prueba = pd.DataFrame({
    "INCOMING": textos_incommin,
    "BANCO": textos_banco,
    "SIMILITUD": similarities,
    "ESPERADO": df["VALOR_ESPERADO"]
        .astype(str)
        .str.strip()
        .str.upper()
})

print("\n========================================")
print("MUESTRA DE CASOS T")
print("========================================")

print(
    df_prueba[
        df_prueba["ESPERADO"] == "T"
    ][
        ["INCOMING", "BANCO", "SIMILITUD"]
    ].head(20).to_string(index=False)
)

print("\nCantidad total de filas:", len(df))

print("\nDistribución:")
print(
    df["VALOR_ESPERADO"]
    .astype(str)
    .str.strip()
    .str.upper()
    .value_counts()
)

print("\nDimensiones embeddings:")
print("Incoming:", emb_incommin.shape)
print("Banco:", emb_banco.shape)
