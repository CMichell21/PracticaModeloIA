# ==========================================
# ANALISIS DE DISTRIBUCION DE SIMILITUDES
# ==========================================

df_analisis = pd.DataFrame({
    "SIMILITUD": similarities,
    "ESPERADO": df["VALOR_ESPERADO"]
        .astype(str)
        .str.strip()
        .str.upper()
})

print("\n========================================")
print("ESTADISTICAS DE SIMILITUD")
print("========================================")

print("\n--- CASOS T ---")
print(
    df_analisis[
        df_analisis["ESPERADO"] == "T"
    ]["SIMILITUD"].describe()
)

print("\n--- CASOS F ---")
print(
    df_analisis[
        df_analisis["ESPERADO"] == "F"
    ]["SIMILITUD"].describe()
)

print("\n========================================")
print("LOS 9 CASOS F")
print("========================================")

print(
    df_analisis[
        df_analisis["ESPERADO"] == "F"
    ].sort_values("SIMILITUD")
)

print("========================================")
